old
{
  "rules": {
    ".read": "now < 1782932400000",  // 2026-7-2
    ".write": "now < 1782932400000",  // 2026-7-2
  }
}

new
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}

или
{
  "rules": {
    "some_path": {
      "$uid": {
        // Allow only authenticated content owners access to their data
        ".read": "auth.uid === $uid",
        ".write": "auth.uid === $uid"
      }
    }
  }
}
